% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_sectional_methods.R
\name{tukeyFences}
\alias{tukeyFences}
\title{Identify outliers using Tukey fences}
\usage{
tukeyFences(
  df,
  valueColumn = "value",
  frequencyColumn = "frequency",
  fenceMultiplier = 1.5
)
}
\arguments{
\item{df}{A data.frame containing a value column and a frequency column.}

\item{valueColumn}{Name of the column in `df` that holds the numeric values.}

\item{frequencyColumn}{Name of the column in `df` that holds the numeric
frequency counts.}

\item{fenceMultiplier}{A single positive number `k` used to scale the IQR.}
}
\value{
A data.frame with `lowerFence`, `upperFence`, and `isOutlier`
  columns appended.
}
\description{
Applies Tukey's method to a value-frequency distribution. Quartiles and IQR
are computed from the frequency-weighted distribution, then lower and upper
fences are calculated as `Q1 - k * IQR` and `Q3 + k * IQR`. Values outside
these fences are flagged as outliers. Assumptions: the method is
distribution-free (no normality assumption) and requires numeric values with
non-negative integer frequencies.
}
