% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomaly_reporting.R
\name{learnPlausibleRangeConsensus}
\alias{learnPlausibleRangeConsensus}
\title{Learn plausible value ranges from univariate consensus voting}
\usage{
learnPlausibleRangeConsensus(
  comparisonDf = NULL,
  results = NULL,
  valueColumn = "value",
  frequencyColumn = "frequency",
  consensusMethod = c("count", "frequency_weighted", "support_weighted"),
  consensusThreshold = 3,
  rangeMethod = c("weighted_quantile", "raw_min_max"),
  lowerProb = 0.001,
  upperProb = 0.999
)
}
\arguments{
\item{comparisonDf}{Optional output from [compareUnivariateOutlierResults()].}

\item{results}{Optional named list of univariate result data.frames. Used only
when `comparisonDf` is `NULL`.}

\item{valueColumn}{Name of the numeric value column.}

\item{frequencyColumn}{Name of the numeric frequency column.}

\item{consensusMethod}{One or more consensus scoring methods. Supported
values are `"count"`, `"frequency_weighted"`, and `"support_weighted"`.}

\item{consensusThreshold}{Numeric threshold used to flag consensus outliers.
For `"count"`, values with score `>= consensusThreshold` are excluded. For
weighted methods, the threshold is applied to the weighted score scale.}

\item{rangeMethod}{One or more range derivation methods. Supported values are
`"weighted_quantile"` and `"raw_min_max"`.}

\item{lowerProb}{Lower quantile probability used when
`rangeMethod = "weighted_quantile"`.}

\item{upperProb}{Upper quantile probability used when
`rangeMethod = "weighted_quantile"`.}
}
\value{
A list with `valueSummary` and `rangeSummary` data.frames.
}
\description{
Computes consensus outlier scores from the binary method flags returned by
[compareUnivariateOutlierResults()] and derives plausible value ranges from
the retained values. The consensus score can be based on a simple vote count,
frequency-weighted votes, or support-weighted votes. Plausible ranges can be
learned using weighted quantiles or raw min/max values.
}
