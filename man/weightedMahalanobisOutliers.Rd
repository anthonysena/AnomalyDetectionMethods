% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_sectional_methods.R
\name{weightedMahalanobisOutliers}
\alias{weightedMahalanobisOutliers}
\title{Identify outliers using weighted Mahalanobis distance}
\usage{
weightedMahalanobisOutliers(
  df,
  valueColumn = "value",
  frequencyColumn = "frequency",
  featureColumns = NULL,
  tailProb = 0.99,
  regularization = 1e-08
)
}
\arguments{
\item{df}{A data.frame containing value/frequency columns and feature columns.}

\item{valueColumn}{Name of the column in `df` that holds numeric values.}

\item{frequencyColumn}{Name of the column in `df` that holds numeric
frequency counts.}

\item{featureColumns}{Character vector of feature column names. If `NULL`,
all columns except `valueColumn` and `frequencyColumn` are used.}

\item{tailProb}{Upper-tail probability used for chi-square cutoff in `(0, 1)`.}

\item{regularization}{Non-negative diagonal ridge term added to covariance
before inversion.}
}
\value{
A data.frame with `weightedMahalanobisDistance`, `distanceCutoff`,
  `outlierProportion`, and `isOutlier` columns appended.
}
\description{
Computes Mahalanobis distance from a frequency-weighted multivariate center
and covariance matrix, without expanding rows by `frequency`. A small
diagonal regularization term is added to stabilize covariance inversion for
near-singular designs. Outliers are flagged by a chi-square distance cutoff.
}
\details{
Assumptions: feature columns are numeric, frequencies are non-negative
integer counts interpreted as case weights, and the inlier distribution is
approximately ellipsoidal.
}
